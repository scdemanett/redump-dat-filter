; Included early in Tauri's NSIS template (before MUI finish page macros).
; The finish-page "readme" checkbox is repurposed for "Create desktop shortcut".
; Without this define it defaults to checked.
!define MUI_FINISHPAGE_SHOWREADME_NOTCHECKED
